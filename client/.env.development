# Configure the dev-server websocket client so React Refresh works in codespaces/previews
# Replace the host below with your forwarded hostname if different.
# WDS_SOCKET_HOST=automatic-pancake-5g7qvwj9v6x735jj-3000.app.github.dev
# Use only the hostname (no protocol, no trailing slash). For preview hosts
# prefer the forwarded hostname and standard HTTPS port 443.
## Disabled dev websocket client — remove comments to re-enable
#WDS_SOCKET_HOST=musical-fiesta-r45g7jqxr7j72wv5r-3000.app.github.dev
#WDS_SOCKET_PORT=443
#WDS_SOCKET_PROTOCOL=wss
#WDS_SOCKET_PATH=/ws
REACT_APP_API_BASE_URL=http://127.0.0.1:8000/api